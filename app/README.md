# app

The progressive web application: activity runner, screens, feedback layer,
adapters (camera, audio, voice, storage). Depends on core and perception —
never the other way around. Anything that touches a browser API belongs here,
not in core or perception.
