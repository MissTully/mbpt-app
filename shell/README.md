# shell

The thin native wrapper around the same web application, for store
distribution and deeper camera access. Deliberately empty: it is added after
the core loop is proven on the web (ADR-001), not before. Nothing in here may
contain application logic.
